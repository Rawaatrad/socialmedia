package Database.Mappers;

public class EntityMapper {
    /*public Page create(ResultSet rs) throws SQLException {
        Page page=new Page();
        rs.next();
        page.setId(rs.getInt(1));
        page.setType(rs.getString(2));
        page.setNameUser(rs.getString(4));
        page.setEmail(rs.getString(3));
        return membre;
    public void create(Member member) {
        String query= "INSERT INTO Member (username,email,password) Values ('"+member.getNameUser()+"','"+member.getEmail()
                +"','"+member.getPassword()+"')";
        Database database=new Database();
        database.updateExec(query);


    }*/
}
